import { styled } from "@mui/system";

export const SwiperItemWrapper = styled ("li")({
    width: "100%",
    flexShrink: 0,

});
export const SwiperImage = styled ("img")({
    width: "100%",
    height: "auto",
    userSelect: "none",
    
});